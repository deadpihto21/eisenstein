#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../wscat/bin/wscat" "$@"
  ret=$?
else 
  node  "$basedir/../wscat/bin/wscat" "$@"
  ret=$?
fi
exit $ret
